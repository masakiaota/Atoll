# テスト実行

- 通常は `python3 scripts/test.py [対象...]` を使う。引数なしは全テスト、`standalone` はアプリを起動しないテスト、`unit` はGUI不要のテスト、`ui` はXCTestである。詳細は `DynamicIslandTests/README.md` を参照する。
- 成功時のstdoutとstderrは空で、終了コード0が成功を表す。成功ログを表示するためだけに再実行しない。
- 異常時は限定量の診断と完全ログの保存先が表示される。必要な箇所だけ検索、取得する。`cat` や `tee` で完全ログをコンテキストへ流さず、`--verbose` は詳細出力が明示的に必要な場合だけ使う。
- ビルドなどの外部コマンドにも `python3 scripts/quiet.py -- <コマンド...>` を使う。パイプで終了コードを失ったり、失敗を成功として報告したりしない。
- 新しいテストも標準の実行経路へ登録する。成功通知の出力や、個別のログ抑制処理を増やさない。
- 実行した対象と実行していない対象を区別する。CIの `unit` 成功を、GUIテストを含む全テストの成功として報告しない。
