# P1 security hardening referent table

Status: final

| 出典 | 目的 | 具体対象 | 役割 | 前後関係 | 候補語 | 初出定義 |
| --- | --- | --- | --- | --- | --- | --- |
| P1 review: fixed loopback RPC impersonation | 固定ポートを先取りしたローカルプロセスにRPCの内容を盗聴・改変させない | アプリ別256-bit token、双方の32-byte nonce、相互HMAC proof、方向別HKDF鍵、ChaCha20-Poly1305、接続・方向・単調sequenceを含むAAD | 開始条件・通信制約 | `secure.begin` → server proof検証 → `secure.complete` → client proof検証 → 全application frameのAEAD | RPC保護チャネル | RPC保護チャネルとは、token自体を送信せずに接続相手を相互確認し、その後の要求・応答・通知を方向別の認証付き暗号で保護するAtollRPC/2セッションを指す。 |
| P1 review: RPC relay/replay | fake serverによる中継時にも平文取得・注入・再送を許さない | bundle ID、server発行session ID、双方nonceを束縛したtranscriptと、各方向で1から厳密に増加するUInt64 sequence | 制約 | handshake中継が成立しても鍵は不明 → ciphertextの転送だけ可能 → sequence不一致・tag不一致で切断 | RPCフレーム順序認証 | RPCフレーム順序認証とは、接続・bundle・方向・sequenceをAEADの追加認証データに含め、欠落・重複・順序変更・別接続への移送を拒否する検査を指す。 |
| P1 review: unauthenticated RPC | 未承認アプリが拡張APIへ到達しない一方、初回承認操作は可能にする | 未認証で許す秘密値を含まない`atoll.requestAuthorization`、15秒・一回限りのhandshake challenge、最大32接続・4 MiB frame | 開始条件・上限 | 既定無効 → 承認要求後切断 → 利用者承認 → 短期handshake → API要求 | RPC承認導線 | RPC承認導線とは、未認証接続には承認候補の登録だけを許し、利用者承認後は短期challengeを完了した保護チャネルだけに拡張APIを許す手順を指す。 |
| P1 review: loopback exposure | LANから拡張APIへ到達できないようにする | Network.framework listenerをloopback interfaceだけへ限定する設定 | 制約 | listener作成 → IPv4/IPv6 loopback限定 → 接続受付 | RPC loopback限定 | RPC loopback限定とは、RPC listenerをこのMac自身のloopback interface以外から到達不能にする制約を指す。 |
| User request: customization remains easy | 利用者と拡張開発者が意図してRPCを有効化・更新できるようにする | 設定画面のapp別tokenコピー・再生成、再生成時の既存接続切断、AtollRPC/2 wire specification | 手段 | 利用者承認 → token受渡し → 規定handshake実装 → 必要時rotation | アプリ別RPCトークン | アプリ別RPCトークンとは、Atollがbundle IDごとにData Protection Keychainへ保存し、利用者が意図した拡張へ一度だけ設定する256-bitの共有秘密値を指す。 |
| Use-case review: one local Atoll app | 権限cacheを複数Atollプロセス間で同期する複雑性を持ち込まず、停止・再開された旧processにも古い権限を使わせない | `LSMultipleInstancesProhibited`、起動時の既存bundle ID process確認、重複instanceを作るrestart機能の削除、transport開始直前のKeychain正本再読込 | システム不変条件 | 通常起動 → 既存instanceがあれば既存windowをactivateして新process終了 → 唯一のinstanceになった後に権限再読込 → transport開始 | Atoll単一インスタンス | Atoll単一インスタンスとは、一人の利用者が同じbuildのAtollを同時に複数動かさない実行条件をLaunch Servicesと起動時検査で強制し、service開始時点のKeychain正本だけを権限根拠にする方針を指す。 |
| Use-case review: ephemeral extension content | ローカルclient由来の一時表示に永続化・復元・process間同期の複雑性を持ち込まない | Live Activity、Lock Screen Widget、Notch Experienceのprocess-local配列、snapshot保存層と未使用observerの削除 | 状態寿命 | client接続 → payload表示 → Atoll終了で破棄 → client再接続時に必要な内容だけ再送 | 一時拡張表示 | 一時拡張表示とは、拡張payloadをAtollの実行中だけ保持し、再起動後はローカルclientを正本として再送させる状態管理を指す。 |
| P1 review: public-name Keychain pre-seeding | 別プロセスが同じservice/account名で用意したlegacy itemを権限情報として読ませない | `kSecUseDataProtectionKeychain`を付けたRPC token、承認記録、global policy、MediaRemote consent | 信頼境界 | code signing access groupによるitem分離 → secure recordのread/write → legacy itemは不一致 | Data Protection Keychain権限台帳 | Data Protection Keychain権限台帳とは、Atollのコード署名に由来するaccess group内だけで秘密値と権限状態を読み書きし、UserDefaultsやlegacy Keychain itemを権限根拠にしない保存層を指す。 |
| P1 review: authorization rollback/failure | 保存失敗や取消直後に古い権限・表示・購読を継続させない | secure authorization record、tombstone、scope縮小時の先行block/disconnect、再承認前token rotation、UserDefaultsの表示用mirror | 状態遷移・終了条件 | block/disconnect → secure save → 成功時だけ再開／失敗時はfail closed | 即時失効 | 即時失効とは、master・機能toggle・app status・scopeが無効になった時点で、永続化の成否にかかわらず既存transport・表示・購読を継続させない処理を指す。 |
| P1 review: bundle ID impersonation | XPCで同じbundle IDを名乗る別アプリに既存承認を継承させない | audit tokenから検証したdesignated requirement、承認済みsecure record、listener queueでのidentity固定、最大64 active接続 | 接続identity制約 | listener queueで署名検証・suspend → MainActorへ引渡し → secure record照合 → resume | XPC署名identity固定 | XPC署名identity固定とは、bundle IDと利用者が承認したdesignated requirementの両方が一致する接続だけにXPC APIを許す判定を指す。 |
| P1 review: web permission revocation and active content | web権限取消後の再導入と、HTML内の任意network・script実行を抑える | secure web toggleの表示直前再検査、non-persistent WKWebsiteDataStore、nonce付きCSP、content rule list、navigation policy、rule生成失敗時blank document | 実行制約・終了条件 | payload受信 → 現在権限照合 → policy生成 → navigation／resource検査 → 許可済み表示 | 拡張Webサンドボックス | 拡張Webサンドボックスとは、現在の権限と宣言済みnetwork scopeからCSP・WebKit規則を生成し、権限取消・規則生成失敗・未許可navigation時にweb contentを表示しない処理を指す。 |
| P1 review: shelf data exposure | Shelfの内容を通常の拡張機能から分離する | master、全体ファイル共有toggle、app承認、app別fileSharing scopeの4条件と接続単位のevent購読 | 開始条件 | 4条件照合 → Shelf要求／通知 → 条件取消で購読解除 | 拡張ファイル共有許可 | 拡張ファイル共有許可とは、認証済み接続にも全体設定とアプリ別scopeを別途要求し、現在も全条件が有効な場合だけShelf APIを処理する判定を指す。 |
| P1 review: Shelf confused deputy | 拡張から任意ローカルpathを読ませたり任意pathへ書かせたりしない | client指定pathの廃止、利用者file picker、単一成分fileName、UUID一時directory、regular file検査、atomic write、64 MiB read上限 | 入出力制約 | 利用者選択またはbounded upload → 安全な一時領域 → bounded read | Shelf入出力境界 | Shelf入出力境界とは、拡張による任意path指定を排除し、対象file type・name・保存先・read量を制限する処理を指す。 |
| P1 review: opaque bundled Mach-O | 出所を証明できない同梱MediaRemoteコードを無断で動かさない | 未使用`NowPlayingTestClient`バイナリとprobeの削除、Data Protection Keychainを根拠とする明示consent、既定無効、旧UserDefaults値を権限根拠にしないruntime block | 開始条件・削除 | 不要実行物は同梱しない → 残すadapterは既定拒否 → 利用者確認 → secure consent保存 → 整合性検査 | 同梱メディアアダプタ許可 | 同梱メディアアダプタ許可とは、必要な同梱Mach-Oの危険性を表示し、利用者が明示的に許可した記録をsecure storeから確認できる場合だけ実行準備へ進む条件を指す。 |
| P1 review: bundled-code replacement and path race | 許可後のscript・framework差替えや検査後path置換を実行へ持ち込ませない | `O_NOFOLLOW`で開いたregular file、固定SHA-256、検証済みscript bytesを`perl -e`へ渡す処理、保持したframework FDを`/dev/fd/0`として利用する処理、初期化済み最小環境 | 実行完全性制約 | exact file open → type・hash検査 → 同じbytes／FDを実行 → handle保持 | FD固定メディア実行 | FD固定メディア実行とは、検査したscript bytesとframework file descriptorそのものを実行時にも使い、検査後に元pathを再openしないMediaRemote adapter起動方式を指す。 |
